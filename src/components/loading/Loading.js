import LoadingImage from "../../img/colapse.png"

function Loading() {
    return (
        <div>
            <img src={LoadingImage} alt="Loading..." />
        </div>
    )
}

export default Loading